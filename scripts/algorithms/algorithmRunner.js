import { visualizationRunner } from "../visualisation/visualizationRunner.js";

export function algorithmRunner(inputData){
  console.log(inputData);

  // visualizationRunner(); // Run the visualization
}